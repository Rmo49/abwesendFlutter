import 'package:flutter/material.dart';

class TableauVerwalten extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return new Scaffold(
        appBar: AppBar(
            title: Text('Tableau verwalten'),
        ),
    );

  }
}