import 'package:flutter/material.dart';

/// Einen Spieler ändern, löschen, Beziehung zu Tableau setzen
class SpielerAdmin extends StatelessWidget {


  @override
  Widget build(BuildContext context) {

    return new Scaffold(
      appBar: AppBar(
        title: Text('Spieler anpassen'),
      ),
    );

  }

}
